# Toko Sembako Online (Laravel)

## Cara jalanin project
1. Clone repo
2. Copy `.env.example` jadi `.env`
3. Set DB di `.env`
4. Jalankan:
   - composer install
   - php artisan key:generate
   - php artisan migrate --seed
   - npm install
   - npm run dev
   - php artisan serve
